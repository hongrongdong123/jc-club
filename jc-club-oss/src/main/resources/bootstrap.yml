spring:
  application:
    name: jc-club-oss
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 116.198.40.220:8848
#        prefix: ${spring.application.name}
#        group: DEFAULT_GROUP
#        namespace:
        file-extension: yaml

